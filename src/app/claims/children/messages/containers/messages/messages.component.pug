.row.messages-view
    .col-12
        pko-messenger(
            [messages]="messages$ | async",
            [focusMessageInput]="instantFocusRequired$ | async",
            (sendMessage)="onSendMessage($event)"
        )
