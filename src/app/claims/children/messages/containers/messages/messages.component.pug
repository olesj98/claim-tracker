.row.messages-view
    .col-12
        pko-messenger(
            [messages]="messages$ | async",
            [scrollToBottom]="true",
            [focusMessageInput]="true",
            (sendMessage)="onSendMessage($event)"
        )
