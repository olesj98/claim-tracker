.container
    h5.text.text--bold.mb-3.mb-md-4 Aby przejść dalej wybierz szkodę, która Cię interesuje:
    pko-claims-list(
        [claims]="claims$ | async",
        [selectedClaimId]="selectedClaimId$ | async",
        (selected)="onClaimSelected($event)"
    )
